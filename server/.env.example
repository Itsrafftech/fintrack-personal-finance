PORT=5000

# PostgreSQL connection
PGHOST=localhost
PGPORT=5432
PGDATABASE=fintrack
PGUSER=postgres
PGPASSWORD=postgres

# Or a single connection string (overrides PG* vars if set)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/fintrack

CLIENT_ORIGIN=http://localhost:5173
